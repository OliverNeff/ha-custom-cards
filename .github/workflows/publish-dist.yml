name: Publish Distribution

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

permissions:
  contents: write
  actions: read

# Verhindert parallele Runs auf dem gleichen Branch (z. B. bei schnellen Pushes)
concurrency:
  group: publish-dist-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build_dist:
    runs-on: ubuntu-latest
    # Endlosschleife vermeiden: eigene Bot-Commits sollen nicht erneut triggern
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Tags/History benötigt

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'yarn'
      - name: Enable Corepack
        run: corepack enable

      - name: Install dependencies (Yarn)
        run: yarn install --frozen-lockfile

      - name: Configure git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

       # 1) Patch-Version in package.json erhöhen
      - name: Bump patch
        run: |
          yarn --version
          yarn version --patch -m "chore: bump version to %s [skip ci]"
          git push origin HEAD:main

      # 2) Build (Passe den Befehl bei Bedarf an, z. B. Vite/Rollup)
      - name: Build
        run: yarn build

      # 3) dist/ als Artefakt bereitstellen (beim Workflow-Run downloadbar)
      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/
          if-no-files-found: error
          retention-days: 14
          
  deploy_to_ha:
    needs: build_dist
    runs-on: [ self-hosted, raspi ]
    env:
      COMMUNITY_ROOT: /opt/homeassistant/config/www/community

    steps: 
      - name: Download Distribution
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: /tmp/ha-cards

      - name: Deploy every *.json to community/<basename>
        run: |
          SRC_DIR="/tmp/ha-cards"

          echo "Contents of ${SRC_DIR}:"
          ls -la "${SRC_DIR}" || true

          shopt -s nullglob
          # suche JSONs im Root und einem optionalen Unterordner
          files=( "${SRC_DIR}"/*.json "${SRC_DIR}"/*/*.json )

          if [ ${#files[@]} -eq 0 ]; then
           echo "No JSON files found under '${SRC_DIR}'." >&2
          exit 1
          fi

          for FILE in "${files[@]}"; do
            NAME="$(basename "$FILE" .json)"            # Zielordner = Basename
            TARGET="${COMMUNITY_ROOT}/${NAME}"
            echo ">> Deploying $(basename "$FILE") -> ${TARGET}/$(basename "$FILE")"

            sudo mkdir -p "$TARGET"
            sudo cp -f "$FILE" "$TARGET/$(basename "$FILE")"
            sudo chown root:root "$TARGET/$(basename "$FILE")"
            sudo chmod 644 "$TARGET/$(basename "$FILE")"
          done
          echo "All JSON cards deployed. Please hard-refresh your browser (Ctrl+F5)."
